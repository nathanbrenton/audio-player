/**
 * Canonical Hiplingo brand artwork.
 *
 * Import the source as a Vite-managed static asset. Both Hiplingo and
 * metadata-editor consume this same package, while each host receives the
 * correct dev/build URL for the one canonical source file.
 */
import hiplingoLogoUrl from "./hiplingo-logo.png";

export { hiplingoLogoUrl };
